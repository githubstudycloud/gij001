# 日程分类器微调项目依赖
# 安装命令: pip install -r requirements.txt

# PyTorch (根据你的CUDA版本选择)
# CPU版本:
# torch>=2.0.0
# GPU版本请访问 https://pytorch.org 获取安装命令

torch>=2.0.0

# Transformers 核心库
transformers>=4.35.0

# 数据处理
datasets>=2.14.0

# 训练加速
accelerate>=0.24.0

# 轻量微调 (LoRA)
peft>=0.6.0

# 评估指标
scikit-learn>=1.3.0

# 进度条
tqdm>=4.66.0

# 数据生成 (可选，用于调用API生成数据)
openai>=1.0.0
requests>=2.31.0
