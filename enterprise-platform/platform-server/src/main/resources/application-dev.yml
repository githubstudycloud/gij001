# 开发环境配置
spring:
  devtools:
    restart:
      enabled: true

logging:
  level:
    com.platform: DEBUG

# 开发环境数据源
# spring:
#   datasource:
#     url: jdbc:mysql://localhost:3306/platform_dev?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=false&serverTimezone=GMT%2B8&allowPublicKeyRetrieval=true
#     username: root
#     password: root
